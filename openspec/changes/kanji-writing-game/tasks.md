## 1. データ層（kanji-dataset）

- [x] 1.1 `KanjiQuestion` 型（kanji / reading / sentence）を定義する
- [x] 1.2 小1配当漢字のみで 10〜20 問のサンプルデータを作成する（各漢字に読みと例文）
- [x] 1.3 例文に対象漢字が確実に含まれることを確認する簡易バリデーション or テストを用意する

## 2. 手書きキャンバス（handwriting-input）

- [x] 2.1 `"use client"` の Canvas コンポーネントを作成し、Pointer Events で線を描画する
- [x] 2.2 タッチ時のスクロール抑止（`touch-action: none` と `preventDefault`）を実装する
- [x] 2.3 「消す」操作でキャンバスをクリアできるようにする
- [x] 2.4 現在の筆跡を `getImageData` で採点用画像として取り出す関数を実装する
- [x] 2.5 未記入時に採点要求してもエラーにならない（空＝0%扱い）ことを確認する

## 3. 採点ロジック（similarity-scoring）

- [x] 3.1 対象漢字を標準フォントでオフスクリーン Canvas に描画し、お手本画像を生成する
- [x] 3.2 画像の2値化とバウンディングボックス抽出を実装する
- [x] 3.3 共通サイズ（例: 64×64）への正規化リサイズを実装する
- [x] 3.4 IoU ベースで一致度を算出し 0〜100% の類似率に変換する
- [x] 3.5 類似率→獲得ポイントの単調変換（0%=0点）を実装する
- [x] 3.6 数問で正規化サイズ・指標・配点をチューニングする

## 4. 音声読み上げ（sentence-audio）

- [x] 4.1 Web Speech API（`SpeechSynthesis`、`lang='ja-JP'`）で元例文を読み上げる関数を実装する
- [x] 4.2 読み上げボタンUIを追加し、虫食い前の完全な例文を読み上げる（何度でも再生可。再生中は中断して最初から）
- [x] 4.3 `speechSynthesis` 非対応時にボタンを無効化し他機能を継続するフォールバックを実装する

## 5. クイズ進行と画面（kanji-quiz）

- [x] 5.1 ゲーム画面（クライアントコンポーネント）を作成し `src/app` に配置する
- [x] 5.2 読みの提示と、例文中の対象漢字を `□` に置換した虫食い表示を実装する（複数出現も置換）
- [x] 5.3 手書きキャンバス・採点・音声再生を画面に統合する
- [x] 5.4 採点時に手書きへお手本を重ねて表示し、その後 正解漢字・類似率（%）・獲得ポイントを表示する
- [x] 5.5 累計ポイントの管理と表示を実装する
- [x] 5.6 「次へ」で次問へ進み、キャンバスをクリアする。最終問題後に累計結果を表示する

## 6. 仕上げと確認

- [x] 6.1 トップページ（`src/app/page.tsx`）からゲームへ導線を付ける（または直接ゲーム画面にする）
- [x] 6.2 `npm run lint` と `npm run build` が通ることを確認する
- [x] 6.3 ブラウザで出題→手書き→採点→音声の一連の動作を手動確認する
